let slideIndices=[];function initializeSlideshow(){let e=document.querySelectorAll(".project");slideIndices=new Array(e.length).fill(0)}function showSlide(e,t){let i=document.querySelectorAll(".project")[e].querySelectorAll(".slide");i.forEach(o=>o.classList.add("hidden")),i[t].classList.remove("hidden"),slideIndices[e]=t}function nextSlide(e){let l=document.querySelectorAll(".project")[e].querySelectorAll(".slide");slideIndices[e]=(slideIndices[e]+1)%l.length,showSlide(e,slideIndices[e])}function prevSlide(e){let l=document.querySelectorAll(".project")[e].querySelectorAll(".slide");slideIndices[e]=(slideIndices[e]-1+l.length)%l.length,showSlide(e,slideIndices[e])}document.addEventListener("DOMContentLoaded",initializeSlideshow);
